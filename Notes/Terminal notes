The Terminal


- \n new line
- \t tab
- ; separar comandos

cat >> show content of a file
cat -n >> all the content of the file
pwd >> current path
ls >> folder in current path
cd >> move to folder
cd /path >> move
cd ... >> go back one folder
dir - >> folders
cd - >> previous folder
cd ~ >> home
ls -lrt >> more details on files and folders
ls -a >> hidden files/folders
ssh >> connect to host
man >> documentation
ls -s>> size
ls -1> files with linebreak
ls -lh> human readable, e.g. kb instead of bytes
ls -rt> sort by time reverse
ls -S> sort by file size
ls -X> sort alphabetical
mkdir> crear carpeta
rm> remove
rm -r> remove directory
rm -rf> remove force
touch> create file
-p> creates file in whole path e.g. mkdir -p one/two/three
cp> copy e.g. cp file new_file
mv> move file path e.g. mv filename filename it renames it
cat ~/.history > 20210422.log
Ls -lrt ~/*.log
Less >> look interactive text
Tail >> look at the bottom of the text
Head >> look at the top
Tail -f >> taile interactivo
Wc >> word count, lineas, palabras y bytes de un archivo
Grep "example.txt" >> selecciona lineas que contienen "string"
Ls -lrt | grep "filename.xyz"
history|grep "rsync"
History 0|grep "git"
History 0>mylog.log
History 0>>mylog.log (adicional)
Bash
Nohup >> corre el scrip aunque cierre la pantalla
echo 'history 0|wc -l' > 001.sh
.sh >> fichero de texto con código shell (bash)
**
Example:
bash 001.sh
001.out
history 0|wc -l
mousepad
echo 'history 0|wc -l' > 001.sh
bash 001.sh > 001.out
***
Which comando (e.g. which bash) >> localiza el comando


alias:
ll >> more details on folders and files

alias is a shortcut

to go back on additional files>>
../../;cat pp.txt

variable de entorno:
$PWD

env >> shows environment

cd / >> goes to root directory

when you open a file you get something like this:

- rw - rw - r -- 1 dsc 18 nov 17:11
r: read
x: execute
w: write
1: # of links (soft and hard)
dsc: owner name
18: byte size
nov 17:11: date modified


28-04
Comandos nuevos:





